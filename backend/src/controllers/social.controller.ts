import { Request, Response, NextFunction } from 'express';
import { SocialService } from '../services/social.service';
export class SocialController { static async leaderboard(req: Request, res: Response, next: NextFunction) { try { const data = await SocialService.leaderboard(Number(req.query.limit) || 50); res.json({ success: true, data, leaderboard: data }); } catch (error) { next(error); } } static async guilds(_req: Request, res: Response, next: NextFunction) { try { const data = await SocialService.guilds(); const guilds = data.map((guild) => ({ ...guild, description: guild.region, members_count: guild.membersCount, total_xp: guild.totalXp })); res.json({ success: true, data, guilds }); } catch (error) { next(error); } } }