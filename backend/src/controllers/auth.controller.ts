import { Request, Response, NextFunction } from 'express';
import { AuthService } from '../services/auth.service';
import { AuthRequest } from '../middleware/auth.middleware';
export class AuthController { static async register(req: Request, res: Response, next: NextFunction) { try { const { email, password, name, guildName } = req.body || {}; if (typeof email !== 'string' || typeof password !== 'string' || typeof name !== 'string') return res.status(400).json({ success: false, error: 'Email, password, and name are required' }); return res.status(201).json({ success: true, data: await AuthService.register(email, password, name, typeof guildName === 'string' ? guildName : undefined) }); } catch (error) { next(error); } } static async login(req: Request, res: Response, next: NextFunction) { try { const { email, password } = req.body || {}; if (typeof email !== 'string' || typeof password !== 'string') return res.status(400).json({ success: false, error: 'Email and password are required' }); return res.json({ success: true, data: await AuthService.login(email, password) }); } catch (error) { next(error); } } static async guest(_req: Request, res: Response, next: NextFunction) { try { return res.status(201).json({ success: true, data: await AuthService.createGuest() }); } catch (error) { next(error); } } static async me(req: AuthRequest, res: Response, next: NextFunction) { try { return res.json({ success: true, data: await AuthService.getProfile(req.user!.id) }); } catch (error) { next(error); } } }